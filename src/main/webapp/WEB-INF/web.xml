<?xml version="1.0" encoding="UTF-8"?>

<web-app
        xmlns="https://jakarta.ee/xml/ns/jakartaee"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="
        https://jakarta.ee/xml/ns/jakartaee
        https://jakarta.ee/xml/ns/jakartaee/web-app_6_1.xsd"
        version="6.1">

    <display-name>
        Servlet CRUD MVC JPA EcoMart
    </display-name>

    <filter>
        <filter-name>adminAuthorization</filter-name>
        <filter-class>vn.iotstar.config.AdminAuthorizationFilter</filter-class>
    </filter>
    <filter-mapping>
        <filter-name>adminAuthorization</filter-name>
        <url-pattern>/admin/*</url-pattern>
        <dispatcher>REQUEST</dispatcher>
    </filter-mapping>

    <filter>
        <filter-name>userAuthentication</filter-name>
        <filter-class>vn.iotstar.config.UserAuthenticationFilter</filter-class>
    </filter>
    <filter-mapping>
        <filter-name>userAuthentication</filter-name>
        <url-pattern>/category</url-pattern>
        <dispatcher>REQUEST</dispatcher>
    </filter-mapping>

    <filter>
        <filter-name>ecoMartSiteMesh</filter-name>

        <filter-class>
            vn.iotstar.config.EcoMartSiteMeshFilter
        </filter-class>

        <init-param>
            <param-name>autoReload</param-name>
            <param-value>false</param-value>
        </init-param>
    </filter>

    <filter-mapping>
        <filter-name>ecoMartSiteMesh</filter-name>
        <url-pattern>/*</url-pattern>
        <dispatcher>REQUEST</dispatcher>
    </filter-mapping>

    <welcome-file-list>

        <welcome-file>
            index.jsp
        </welcome-file>

    </welcome-file-list>

    <session-config>

        <session-timeout>30</session-timeout>

        <tracking-mode>
            COOKIE
        </tracking-mode>

    </session-config>



</web-app>
